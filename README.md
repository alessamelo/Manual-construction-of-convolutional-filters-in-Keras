# Manual-construction-of-convolutional-filters-in-Keras
This repository contains an implementation of a simple CNN built with Keras, designed to demonstrate how convolutional filters work in image processing. Instead of allowing the model to learn the filters automatically, the kernel values of a convolutional layer are manually defined to detect specific patterns in images, such as edges.
